// Legacy export — the active form lives at /app/hr/jobs/new/. Re-exported so
// any stale imports keep type-checking; the page at this path is now a
// redirect, so this file is dead code in practice.
export { JobForm } from '../../hr/jobs/new/JobForm'
