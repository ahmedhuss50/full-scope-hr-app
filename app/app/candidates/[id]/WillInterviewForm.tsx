// Legacy export — the active form lives at /app/hr/applications/[id]/.
// Re-exported so any stale imports keep type-checking; the page at this path
// is now a redirect, so this file is dead code in practice.
export { WillInterviewForm } from '../../hr/applications/[id]/WillInterviewForm'
